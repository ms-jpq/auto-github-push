---
name: AGP

on:
  push:
  schedule:
    - cron: "0 0 * * *" # daily

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: AGP
        uses: ms-jpq/auto-github-push@346610e12b0394f20a5dea3d13f46e0dd7724b22
        with:
          username: ${{ env.GITHUB_ACTOR }}
          token: ${{ secrets.CI_TOKEN }}
