---
name: AGP

on:
  push:
  schedule:
    - cron: "0 0 * * *" # daily

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: AGP
        uses: ms-jpq/auto-github-push@4a40a2f4bd33524de08e45e50fcfc08fd2ccb93d
        with:
          token: ${{ secrets.CI_TOKEN }}
